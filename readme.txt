Для сборки проекта в исполняемый файл (при использовании компилятора C++ из коллекции GCC), находясь в корне проекта, введите команду
c++ parsing/*.cpp types/*.cpp execution/*.cpp main/*.cpp -o interpreter.exe